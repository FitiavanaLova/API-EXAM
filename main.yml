info:
  title: STD24074
  version: 1.0.0
servers:
  - url: 'http://localhost:8000'
paths:
  /ping:
    get:
      responses:
        200:
          description: “This is a specification of STD24074”
          content:
            text/plain:
              schema:
                type: string
                example: pong
